---
import { contact } from "../../data/contact";
import SectionFrame from "./SectionFrame.astro";
---

<SectionFrame id={contact.id} shape="galaxy" kicker={contact.kicker} title={contact.title} body={contact.body}>
  <ul class="mt-7 flex flex-col gap-x-[26px] gap-y-3.5 text-[19px] sm:flex-row sm:flex-wrap">
    {
      contact.links.map((l) => (
        <li>
          <a
            href={l.href}
            class="link-underline"
            {...(l.external ? { target: "_blank", rel: "noopener noreferrer" } : {})}
          >
            {l.label}
            {l.external && <span class="sr-only"> (opens in a new tab)</span>}
          </a>
        </li>
      ))
    }
    <li>
      {/* Opens the email dialog in components/ContactFormDialog.astro; wired up by scripts/contact.ts. */}
      <button type="button" data-email-open class="link-underline cursor-pointer">Email</button>
    </li>
  </ul>
</SectionFrame>
